***Input*** ---> Mouse and keyboard <br>
***Outpue*** ---> Monitor

### Computer is a mechine who can calculate something
1. First Computer name is abacus
2. binary logic invented by leibniz
3. first machanical computer invented by babbage and it's real to coding
4. In , cpu  and hard disk, the number of bits in a single cell of RAM does  have to be the same as the number of bits in a single cell of the hard disk.
5. in general 32 bit computer means, the number of bits in a single cell of ram and hard disk is 32 bit

## CPU box

### 1. Ram
```py
1. Primary Memery
```
### 2. Cpu
```py
Only 7 operation do (+,-,*,/,&,or and not)

two Part in a cpu (Processing Unit and Register Set)

registered set এর মধ্যে একটা pointer register থাকে , যা ram এর কোনো একটা cell কে point করে, Processing Unit ওই Pointer register এর পয়েন্ট করা cell এর value কে নিয়ে registered set এ রাখে, register set ওই 8 টা বিট রাখার জন্য একটা pattern ব্যবহার করে, 3 টা register মিলে এই 8 bit কে রাখে, প্রথম register এ রাখে দুই বিট যা হলো operation এর ধরন, like sum or division or multiplication.. এবং বাকি দুই register এ ৬ বিট থাকে যা হলো value, যাদের operation করবো
```
### 3. Hard Disk
```py
1. Permanent Memory
2. Secondary Memory
```
### 4. Mother Board
```markdown
    1. Connect the cpu with ram and hardDisk with ram
    2. flow current every where through the Mother board
```
### 5. Power Supply